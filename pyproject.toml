[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-world-lantern-hollow"
dynamic = ["version"]
description = "A small persistent pixel world."
requires-python = ">=3.11"
license = "MIT"
dependencies = ["agent-world @ git+https://github.com/Munable/agent-world.git@7d3609853db34f3402754bd0860fee96115a9de1"]

[project.scripts]
lantern-hollow = "lantern_hollow.server:main"

[tool.setuptools.packages.find]
include = ["lantern_hollow*"]

[tool.setuptools.package-data]
lantern_hollow = ["web/*"]

[tool.setuptools.dynamic]
version = {attr = "lantern_hollow.__version__"}
